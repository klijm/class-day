
1.  再git中  输入;
 ssh-keygen -t rsa -C "注册邮箱"  //  自己的注册邮箱;
	小节	
	1.通过在git中,输入的邮箱后提示的文件地址信息,找到秘钥(公开的秘钥)
	2.在github上,创建一个新的秘钥,输入邮箱地址给的秘钥;即可
	3.在git中,输入:ssh -T git@github.com   测试是否已经关联;  出现hi即可; 




2.  再github中,注册秘钥,然后再c盘;.ssh文件中找到秘钥; 
3.查看关联
ssh -T git@github.com

ok了   下面就是记住:  操作命令!



工作区(本地)         暂存区           版本区
a文件开发了一半,   就让你开发b   你可以把a放到  暂存区,  等b开发完了,再从暂存区,里找到a文件;


进入盘符
cd d:
进入文件夹
cd class
查看当前文件夹有哪些文件
ls
工作区放到暂存区;
git add
******  git add .   空格点 
git add 文件名
查看状态
git status 


/*
	再工作区a文件时,查看状态  是红色的
	提交到暂存区, 再查看状态是  绿色的
*/
小技巧:  再本地(工作区) 修改文件时,  查看状态是红色的;  如果修改了多个文件;  要提交多个文件?  怎么半????
提交git add .   提交多个文件;  (批量提交文件)  =>到暂存区!

技巧:  每次提交,都要查看一下状态!!!


暂存区是不安全的;  最终都要提交到   版本区;
如何提交到版本区???


git commit -m  "注释"  暂存区  提交到  版本区   这个-m  是"本次提交内容的版本";
每一次提交到版本区; 都会生成一个独一无二的;    ID     

我要看看 我的版本号  
git log   查看版本
进入版本  返回  ==>> q  键



******如果修改了提交过的文件的内容;   也就是说再版本区有这个文件;   你修改了
可以使用快捷提交版本区的方法;
git commit -a -m  "注释"  ;;;;;
    



再github上创建一个项目;
并再自己的本地(工作区),找一个文件储存;  *******************产生联系;;;;;;;
git clone  克隆文件;


提交上传到
*****git push origin master    暂存区,提交上传到 版本区;;;
把已经完成的页面直接上传到;;;版本区;;;

设置贡献者;  步骤
$ git config --global user.name "你的名字"  
$ git config --global user.email "你的邮箱"  
****以前设置过;


清屏:  clear
 


还有一堆的命令;

再本机新建的文件夹;  只是并不是再   git上可以进入;
再文件夹中,新建html文件,才算的上是   工作区; 
再工作区,修改,更改;

举个例子;
你新建一个html文件, 加了一个div,提交到暂存区;
然后你又更改了html文件;  加了style ;



现在这三个区的  状态分别为什么????
想明白了么?

**** 查看区别
工作区与暂存区    区别
git diff
暂存去与版本区    区别
git diff --cached
****它们之间的区别;  就是暂存区,有的东西;  版本区  一个也没有!
如果说提交完成了  push之后,   它们是没有区别的;;;;
工作区与版本区    区别
git diff master
****他们之间的区别:   工作区的东西的最全的;;  版本区一个也没有!

三区


403报错;  为什么?
因为你远程的仓库都没有了;  你往那么上传呢?  
网上建立了一个,远程库;  
我要操作这个远程库;**********就把这个库clone下来;    再本地找一个文件夹,存起来;;;   
****在网上的操作与我有TM  什么关系???  你fork; 删除项目; 都是你的事;     *** 只要我clone下来,就与我有关系了;;;
只要clone下来;  就有了三个区;;; 工作区;;;暂存区;;;版本区;;;
这个库;;;clone下来;;;只要每天都传一点东西;;;  ok?
 



***********撤销;;;
把暂存区的文件撤销到; 工作区
git reset HEAD 文件名   是撤销刚才的 提交命令;;;  

git diff  工作区,暂存区 => 区别;;;
******有什么用????  就是一不小心把工作的代码,全部删除了 ;;;  通过暂存区,撤销回来;;; 就这个意思;;;

******工作中,,,,千万别搞反了;;;;;;不然死人;;
工作区   需要暂存区还原  
git checkout -- 文件名;;; 这个是把刚才的提交的文件  拷贝到工作区;;

*******如果有多个文件,提交到版本区之后发现还有文件忘记提交;;这个时候使用;;
1 把遗漏的文件提交到暂存区;
2 git commit -m "注释" --amend
就是把最近一次的版本区拉回,和暂存区的代码一起形成新的版本;;;

**** 怎么理解?  有两个文件;html和js文件; 你只提交了,html文件,但是忘记了js;  怎么办????它俩是一起的呀!   首先把js这个遗漏的文件提交到暂存区;;然后用git commit -m "注释" --amend  命令把最近一次的提交的文件拉回;;;合并;;;并且;;;




没有错,  只是远程仓库没有了;;  在clone  一次就好了;;;这个不怕;;;


在git clone后的项目下来;;;然后你又新建了一个文件;;; 不要在文件夹中删除;;即使你这样做了;; 也没用;;没有完全删除;;;那样的话;;只是在工作区删除了;; 没有在那两个删除;;;



******git 命令
git rm 文件名         工作区手动在文件夹删除,也想删除暂存区的
git rm -f 文件名      删除工作区和暂存区的文件
rm -rf  文件夹名字    删除整个***文件夹***


************如果误删了怎么办????
误删了文件;js文件;html文件;  ???
/////****恢复呗;;;;

------------****************
必须要提交到版本;;;;;;;;;;
还原:
	还原指定的文件;;;
	git checkout 历史记录ID 文件名
还原整个历史记录   用的也是最多的   一次性全部还原
	git reset --hard 历史记录ID   
	


同步到远程仓库

查看仓库的名字;
git remote  
看看下载,上传的地址 是不是一样;;;
git remote -v    

git push origin master  将版本区的文件上传到 仓库



**********想要删除已经提交的文件;在本地文件夹清空后;  直接再 push一遍即可; 远程仓库就清空了;;;;



*************协作者;;;;;
	多人协作开发
进入项目  点击coll
添加合作者;
合伙人需要邮箱确认;
合伙人需要把项目clone到本地;
在本地修改  在提交

遇到了冲突
git frtch 拉去远程仓库的代码
对比冲突  git diff master origin/master   查看对比冲突;;;; 
合并 git merge origin/master   合并冲突,人为删除冲突代码 
再次提交代码  到版本区;   最后push;;;;
